# Board config for the M5StickS3 (saves the five Tools-menu options).
# SPIFFS/NVS partition needed: the pet persists to flash.
default_fqbn: esp32:esp32:esp32s3:PSRAM=opi,FlashSize=8M,PartitionScheme=default_8MB,CDCOnBoot=cdc
